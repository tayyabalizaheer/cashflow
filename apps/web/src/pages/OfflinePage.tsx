export function OfflinePage() {
  return (
    <main className="center-page">
      <section className="form-card">
        <p className="eyebrow">Offline</p>
        <h1>You are offline</h1>
        <p>Previously viewed read-only pages may still be available. Financial changes are disabled until the connection returns.</p>
      </section>
    </main>
  );
}
